1. 数据不完整,没有跨buf
2. 数据跨buf,并且不完整,并且不能计算包长
3. 数据跨buf,并且不完整,但是能计算包长
4. 数据跨buf,跨buf的请求完整,但是buf2中有不完整的包
4. 恰好使用完一个buf
5. 恰好时用完两个buf

1. 数据没有一次发完

1. 内存泄露
2. client关闭socket,有未发送的数据

1. 过期连接清除

1. 性能测试
2. 多线程测试
